import { BurgerDirective } from './burger.directive';

describe('BurgerDirective', () => {
  it('should create an instance', () => {
    const directive = new BurgerDirective();
    expect(directive).toBeTruthy();
  });
});
